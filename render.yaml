services:
  - type: web
    name: grabit-api
    env: python
    region: frankfurt
    plan: free
    buildCommand: pip install -r requirements_api.txt
    startCommand: uvicorn api_v3:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
    healthCheckPath: /api/health